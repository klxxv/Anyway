[package]
name = "json-repair"
version = "0.1.0"
edition = "2021"
description = "Auditable LLM JSON boundary extraction, bounded repair, and strict deserialization."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
