# Circuit Orthogonal

A declarative Research Canvas `EdgeStylePlugin` that uses strict 90-degree
routing. Supporting evidence is cyan, refutation is red and dashed, controls
are muted, and measurement relations are purple.

The package requests no permissions and exposes only `edge.style.register`.
