# Export Suite

Declares `project.export` and contributes PDF, SVG, and PNG commands. The host owns
the renderer and writes only to a path selected by the user; this declarative plugin
never receives direct filesystem access.
