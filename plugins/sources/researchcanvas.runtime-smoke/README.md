# Runtime Smoke

This deterministic WAT fixture uses the same ABI as the Rust and C++ SDKs. It
lets the desktop installer and plugin store prove installation, SHA-256
registration, VM execution, JSON output, and fuel accounting without requiring
a language toolchain during application tests.
