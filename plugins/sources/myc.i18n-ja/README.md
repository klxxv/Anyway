# Japanese UI example

Version 1.0.1 adds localized, parameterized workspace Toast messages.

Community locale bundles replace known message keys and fall back to built-in
English for keys not yet translated. Enabling/disabling the package updates the
language list without rebuilding Research Canvas.
